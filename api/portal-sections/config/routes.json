{
  "routes": [
    {
      "method": "GET",
      "path": "/portal-sections",
      "handler": "portal-sections.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/portal-sections/count",
      "handler": "portal-sections.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/portal-sections/:id",
      "handler": "portal-sections.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/portal-sections",
      "handler": "portal-sections.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/portal-sections/:id",
      "handler": "portal-sections.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/portal-sections/:id",
      "handler": "portal-sections.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
